import React from 'react';
import styled from 'styled-components';

const StyledWrapper = styled.div`
  background-color: white;
  min-height: 1000px;
`;

const BrandIdentity = () => {
  return <StyledWrapper>BrandIdentity</StyledWrapper>;
};

export default BrandIdentity;
